const mongoose = require('mongoose')
const { doMigration } = require('../../../database/migrations')

describe('Database Migrations', () => {
    it('Should save all the data into the database', async () => {
        
    })
})